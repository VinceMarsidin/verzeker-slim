# PostgreSQL connection (required for reviews, contact form, and auth persistence)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/verzekerslim

# Better Auth — generate with: pnpm dlx @better-auth/cli secret
BETTER_AUTH_SECRET=your-secret-here
BETTER_AUTH_URL=http://localhost:3000
